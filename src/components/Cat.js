// ---------- Imports ---------- //
import React from 'react';

const Cat = (props) => {
    return (
        <div>
            <h2>Generate a Cat!</h2>
            <button>Is a Button</button>
        </div>
    );
};

export default Cat;