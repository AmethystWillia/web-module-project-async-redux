// ---------- Imports ---------- //
import React from 'react';
import { connect } from 'react-redux';

// ---------- Component ---------- //
const Cat = (props) => {
    // Renders content
    return (
        <div>
            <h2>Generate a Cat!</h2>
            <button>Is a Button</button>
        </div>
    );
};

export default Cat;